# Git workflow

Before creating a commit or pushing changes:

1. Review `git diff` and `git status`.
2. Write a concise commit message that accurately summarizes the changes.
3. When the user asks to commit or push, create the commit and push the current branch without waiting for additional approval.
4. Never use `git push --force` unless the user explicitly requests it.
